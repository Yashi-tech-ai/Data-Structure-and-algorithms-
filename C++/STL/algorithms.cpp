#include<bits/stdc++.h>
using namespace std;
/*  // sorting an array in stl 
sort(a,a+n) = here a is included that is the first index and a+n points to the space after last index which is not included 
sort(v.begin(),v.end());
sort(a+2, a+4);
sort(a,a+n,greater<int>); // sorts in descending order 
pair<int,int> a[]= {{1,2},{2,1},{4,1}};
  
sort it according to  second element and if the second element is same then sort it according to first element 

sort(a,a+n,comp);

//{4,1},{2,1},{1,2}

int num = 7
int cnt = _builtin_popcount();

long long num = 32433645767
int cnt = _builtin_popcountll();

string s = "123"
do{
    cout << s << endl;
}while(next_permutation(s.begin(),s.end()))
*/